def repeat(val, x=10){
  for(i in 0..<x){
    println val
  }
}

pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage('build') {
            steps {
	    	repeat
            }
        }
    }
}
